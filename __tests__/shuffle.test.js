
const shuffle = require("../src/shuffle");

describe("testing shuffle, () => {
  test('shuffle should return an array', () =>{
    expect(array.isArray(['value'])).toBe(true);
  })
  test('shuffle should return an array of 10', () =>{
    expect(result.length).toBe(10);
  });
});
